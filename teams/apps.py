"""
Author: [Your Name] - Teams page: list, search, detail, email, schedule meeting, skills, dependencies
"""
from django.apps import AppConfig


class TeamsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'teams'
